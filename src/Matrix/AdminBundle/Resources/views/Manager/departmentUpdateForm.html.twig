{% extends "MatrixAdminBundle::base.html.twig" %}
{% block content %}
    {% for flashMessage in app.session.flashbag.get('notice') %}
        <div class="flash-notice">
            {{ flashMessage }}
        </div>
    {% endfor %}
    <form action="{{ path('department_update') }}" method="post">
        <input type="text" name="department-name" required="true" value="{{ department.name }}"/>
        <input type="hidden" name="department-id" value="{{ department.id }}"/>
        <input type="submit" value="Update" />
    </form>
{% endblock %}