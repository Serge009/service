{% extends "MatrixAdminBundle::base.html.twig" %}
{% block content %}
    <table>
        <thead>
        <tr>
            <th>Name</th>
            <th>Company</th>
        </tr>
        </thead>
        <tbody>
        {% for division in divisions %}
            <tr>
                <td>{{ division.name }}</td>
                <td>{{ division.company.name }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}